# TODO: insert resources here

$origins = {"o1"="o1id","o2"="o2id","o3","o4"}
$routeorigins=["o2","o3"]


foreach($origin in $origins)
    {
        foreach($route in $routeorigins)
            {
                if($route -eq $origin.key)
            }
    }